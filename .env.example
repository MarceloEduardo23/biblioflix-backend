# Segredo do JWT — DEVE ser igual no gateway e no auth-service.
# Em produção, gere algo aleatório (ex.: openssl rand -base64 32).
AUTH_SECRET=dev-secret-troque-em-producao

# Origem do frontend liberada no CORS do gateway.
FRONTEND_ORIGIN=http://localhost:3000
